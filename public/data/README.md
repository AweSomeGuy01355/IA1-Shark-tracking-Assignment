Place the dataset CSV file (`qld-shark-control-program-catch-by-species-2017.csv`) in this folder.

The visualization page (`map.html`) expects the file to be available at `/data/qld-shark-control-program-catch-by-species-2017.csv`.

The repository does not ship with the actual dataset due to size and licensing; download it from the Queensland Government data portal or other source and add it here.